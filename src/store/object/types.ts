export const SET_NAME = 'SET_NAME';

// The setName action's expected type
export type SetNameTypes = {
  type: string,
  payload: string,
};

// The string state's expected type
export type ObjectType = {
  name: string,
};
